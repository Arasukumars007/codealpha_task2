# This directory stores processed feature files (.npz).
# These are generated by running the training pipeline:
#   python run.py preprocess
#
# Files are excluded from Git because they are large (400-500 MB).
